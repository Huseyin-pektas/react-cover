import React,{map} from 'react'

const MapArrayFunction = ({skils}) => {
    const SkillList =skils.map((skils)=>console.log(SkillList,"burada ne var"))


  return (
    <div>
      <p>burası map işlemi yapılacak yerdir</p>
    </div>
  )
}

export default MapArrayFunction
